[project]
name = "mcp-aggregator"
version = "2.0.1"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115,<0.142",
    "uvicorn[standard]>=0.52,<0.53",
    "mcp>=2,<3",
    "aiosqlite>=0.20,<0.23",
    "httpx>=0.27,<0.29",
    "anyio>=4.11,<5",
    "sse-starlette>=2.1,<3.5",
    "python-multipart>=0.0.9,<0.0.33",
    "itsdangerous>=2.2.0,<2.3",
    "sqlmodel>=0.0.38,<0.0.40",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "aggregator"

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
]
ignore = [
    "E501", # line length -- the formatter handles wrapping; long strings/URLs still slip through
]

[tool.ruff.lint.isort]
known-first-party = ["aggregator"]
