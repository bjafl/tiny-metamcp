[project]
name = "tiny-metamcp"
version = "2.1.0"
requires-python = ">=3.14"
dependencies = ["mcp-aggregator"]

[tool.uv.sources]
mcp-aggregator = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]
exclude = ["packages/webui"]